# Stub — satisfies the build-time dependency declared in esp_ot_br_server/CMakeLists.txt.
# The header is included by esp_br_web.c but no symbols from it are actually used.
idf_component_register(
    INCLUDE_DIRS "include"
)
