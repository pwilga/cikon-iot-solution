menu "Cikon IoT Supervisor Button Adapter"
    config BUTTON_GPIO
        int "Button GPIO Pin"
        default 0 if IDF_TARGET_ESP32
        default 9 if IDF_TARGET_ESP32C3
        default 0
        help
            GPIO pin connected to the physical button.
            Default: GPIO0 (BOOT button on most dev boards)

    config BUTTON_ACTIVE_LEVEL
        int "Button Active Level"
        default 0
        range 0 1
        help
            Active level of the button:
            0 = Active low (button connects to GND when pressed)
            1 = Active high (button connects to VCC when pressed)
endmenu
